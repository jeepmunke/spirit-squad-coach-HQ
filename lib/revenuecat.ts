// RevenueCat is not available in Expo Go — requires a custom dev build.
// This file stubs the API so the rest of the app compiles cleanly.

export function initRevenueCat(_userId?: string) {}

export async function checkProEntitlement(): Promise<boolean> {
  return false;
}

export async function getOfferings() {
  return null;
}
